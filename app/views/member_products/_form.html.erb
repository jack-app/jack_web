<%= form_with(model: member_product, local: true) do |form| %>
    <% if member_product.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(member_product.errors.count, "error") %> prohibited this product_link from being saved:</h2>

          <ul>
            <% member_product.errors.full_messages.each do |message| %>
                <li><%= message %></li>
            <% end %>
          </ul>
        </div>
    <% end %>

    <div class="form-group">
      <label>メンバー</label>
      <%= form.collection_select :member_id, Member.all, :id, :name, class: 'form-control' %>
    </div>
    <%= form.hidden_field :product_id, class: 'form-control' %>

    <div class="actions">
      <%= form.submit %>
    </div>
<% end %>
